# Windows Chrome CDP 地址
CDP_HTTP=http://127.0.0.1:9222

# CDP 代理目标（Windows Chrome 地址）
CDP_PROXY_TARGET=localhost:9222

# CDP 代理监听地址
CDP_PROXY_LISTEN=127.0.0.1:9222

# 上游 HTTP 代理（可选，WSL2 常见场景）
# HTTP_PROXY=http://192.168.31.215:7890
